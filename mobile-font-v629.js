(()=>{
  // Mobile typography is controlled by mobile-final.css.
  // This file intentionally does not inject a second font-size override.
  // Keeping one source of truth prevents the delayed JS override from making text small again.
})();
