(()=>{
'use strict';
if(window.__NWCEO_CORE233)return;window.__NWCEO_CORE233=true;
const URL='https://vbkuvexyqehmpeeejqbh.supabase.co',KEY='sb_publishable__nczNPWr3do_hqi6MCS0AQ_fjYCxH_Gk';
const sb=window.__NWCEO_DB||window.supabase?.createClient(URL,KEY);if(!sb)return;
const $=(s,r=document)=>r.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const auth=async()=>((await sb.auth.getUser()).data?.user||null),main=()=>$('#app')?.querySelector('main')||$('#app');
const fmt=d=>d?new Date(String(d).slice(0,10)+'T00:00:00').toLocaleDateString('el-GR'):'—';
const rowList=(rows,empty,fn)=>rows.length?`<div style="display:grid;gap:10px;margin-top:14px">${rows.map(fn).join('')}</div>`:`<div class="card" style="margin-top:14px;color:#667085;text-align:center">${empty}</div>`;
async function inventory(){const u=await auth();if(!u)return;const r=await sb.from('inventory_products').select('id,product_name,sku,category,stock,minimum_stock,unit_price,active,notes').eq('user_id',u.id).order('product_name');const m=main();if(!m)return;m.innerHTML=`<section id="nw233"><div class="pagehead"><div><h1>📦 Αποθήκη</h1><p>Απόθεμα, χαμηλό απόθεμα, παραλαβές και πωλήσεις.</p></div></div>${r.error?`<div class="card"><b>Δεν φορτώθηκε η Αποθήκη.</b><p>${esc(r.error.message)}</p></div>`:rowList(r.data||[],'Δεν υπάρχουν προϊόντα στην αποθήκη.',x=>`<article class="rowcard" style="${Number(x.stock)<=Number(x.minimum_stock)?'border-color:#e7c7c7;background:#fff5f5':''}"><div><b>${esc(x.product_name)}</b><span>${esc(x.category||'')} · SKU ${esc(x.sku||'—')}</span></div><p>Απόθεμα: <b>${x.stock}</b> · Ελάχιστο: ${x.minimum_stock} · Τιμή: ${Number(x.unit_price||0).toFixed(2)} €</p><small>${esc(x.notes||'')}</small></article>`)}</section>`}
async function reports(){const u=await auth();if(!u)return;const [c,f,o,p,t]=await Promise.all([sb.from('clients').select('id',{count:'exact',head:true}).eq('user_id',u.id),sb.from('finance_transactions').select('amount,transaction_type').eq('user_id',u.id),sb.from('orders').select('id',{count:'exact',head:true}).eq('user_id',u.id),sb.from('programs').select('id',{count:'exact',head:true}).eq('user_id',u.id),sb.from('trainings').select('id',{count:'exact',head:true}).eq('user_id',u.id)]);const inc=(f.data||[]).filter(x=>x.transaction_type==='income').reduce((s,x)=>s+Number(x.amount||0),0),ex=(f.data||[]).filter(x=>x.transaction_type==='expense').reduce((s,x)=>s+Number(x.amount||0),0);const m=main();if(!m)return;m.innerHTML=`<section id="nw233"><div class="pagehead"><div><h1>📊 Reports</h1><p>Η κεντρική εικόνα της επιχείρησης.</p></div></div><div class="grid stats"><article>👥<b>${c.count||0}</b><small>Πελάτες</small></article><article>🛒<b>${o.count||0}</b><small>Παραγγελίες</small></article><article>📚<b>${t.count||0}</b><small>Εκπαιδεύσεις</small></article><article>💰<b>${inc.toFixed(2)} €</b><small>Έσοδα</small></article><article>💸<b>${ex.toFixed(2)} €</b><small>Έξοδα</small></article><article>📈<b>${(inc-ex).toFixed(2)} €</b><small>Καθαρό</small></article></div></section>`}
function route(){if(location.hash==='#nw-inventory')return inventory();if(location.hash==='#nw-reports')return reports()}
window.addEventListener('hashchange',()=>setTimeout(route,30));if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(route,250));else setTimeout(route,250);
})();
