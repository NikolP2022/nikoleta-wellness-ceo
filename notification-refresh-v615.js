(()=>{'use strict';
const URL='https://vbkuvexyqehmpeeejqbh.supabase.co';
const base64url=s=>{let x=s.replace(/-/g,'+').replace(/_/g,'/');while(x.length%4)x+='=';const b=atob(x),a=new Uint8Array(b.length);for(let i=0;i<b.length;i++)a[i]=b.charCodeAt(i);return a};
async function refreshPush(show=true){try{if(!('serviceWorker'in navigator)||!('PushManager'in window)||!('Notification'in window)){if(show)alert('Η συσκευή/πρόγραμμα περιήγησης δεν υποστηρίζει push ειδοποιήσεις.');return}const p=await Notification.requestPermission();if(p!=='granted'){if(show)alert('Οι ειδοποιήσεις δεν έχουν άδεια.');return}const reg=await navigator.serviceWorker.register('./sw-v602.js',{updateViaCache:'none'});await reg.update();const sb=window.supabase.createClient(URL,'sb_publishable__nczNPWr3do_hqi6MCS0AQ_fjYCXhGk');const token=(await sb.auth.getSession()).data.session?.access_token||'';const r=await fetch(`${URL}/functions/v1/register-push`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'}});const j=await r.json();if(!j.publicKey)throw new Error('Δεν ελήφθη VAPID key');let old=await reg.pushManager.getSubscription();if(old)await old.unsubscribe();const sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:base64url(j.publicKey)});const s=sub.toJSON();const save=await fetch(`${URL}/functions/v1/register-push`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({endpoint:s.endpoint,keys:s.keys})});if(!save.ok)throw new Error(await save.text());if(show)alert('🔔 Οι ειδοποιήσεις ανανεώθηκαν επιτυχώς σε αυτή τη συσκευή.')}catch(e){if(show)alert('❌ Δεν ανανεώθηκαν οι ειδοποιήσεις: '+(e.message||e))}}
window.refreshPush=refreshPush;
document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;const t=(b.textContent||'').toLowerCase();if(t.includes('ενεργοποίηση ειδοποιήσεων')){e.preventDefault();e.stopPropagation();refreshPush(true)}},true);
})();
